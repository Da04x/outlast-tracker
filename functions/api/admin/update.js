export async function onRequestPost({request,env}){if(!env.ADMIN_KEY||request.headers.get('x-admin-key')!==env.ADMIN_KEY)return Response.json({error:'Unauthorized'},{status:401});let b;try{b=await request.json()}catch{return Response.json({error:'Invalid JSON'},{status:400})}let n=Number(b.count);if(!Number.isInteger(n)||n<0||n>400)return Response.json({error:'Count must be an integer from 0 to 400'},{status:400});if(!env.TRACKER_KV)return Response.json({error:'TRACKER_KV is not configured'},{status:503});await env.TRACKER_KV.put('state',JSON.stringify({count:n,mode:'MANUAL',updatedAt:new Date().toISOString()}));return Response.json({ok:true,count:n})}